{
  "name": "site-performance",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "npm run preview --prefix client -- --host 0.0.0.0 --port 4174"
  }
}

